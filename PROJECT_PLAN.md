# 전자식 차일드락 제어 SW — 프로젝트 계획

이 문서는 `OEM_Sample/OEM-SWR-001_OEM SW 요구사항 사양서.docx`(이하 "OEM 사양서")를 입력으로, `CLAUDE.md`의 분석→설계→구현→테스트 생명주기와 등록된 서브에이전트/스킬을 이용해 소프트웨어를 개발하기 위한 단계별 작업 계획이다. 진행하면서 계속 갱신한다.

## 0. 워크플로우 원칙

- Phase(단계 묶음)마다 **분석 → 설계 → 구현 → 테스트** 4단계를 모두 거친다. 이전 Phase가 완전히 병합되기 전에는 다음 Phase에 착수하지 않는다.
- 각 단계 산출물은 브랜치로 작업하고 PR을 연다. 사용자가 PR을 리뷰하고 승인하면 그때 `main`에 병합한다(`CONTRIBUTING.md` 정책: `CI / quality-gates` 통과 필수, 승인 리뷰어 0명이지만 이번 프로젝트에서는 사용자 본인이 매 단계 실질적 리뷰를 수행).
- 각 산출물은 OEM 사양서 §8의 공급자 게이트(G1~G4)와 대응한다.

## 1. OEM 사양서 요약

- **대상**: 대한민국 판매용 가상 차량의 후석 좌/우 전자식 차일드락 제어 SW. 실행환경은 PC/SIL(Python)과 Web 시뮬레이터. HIL/실차/ECU/인증은 범위 밖.
- **요구사항**: ASIL B 입력 4건(OEM-SR-001~004), QM 기능 7건(OEM-FR-001~007), 비기능 2건(OEM-NFR-001~002).
- **외부 인터페이스**: 9건(OEM-IF-001~009) — 차량 신호(속도/기어/충돌/접근위험/화재/과온/성인탑승/ISOFIX/점화/센서고장), 운전자 명령, 액추에이터 출력, 표시(Web) 인터페이스.
- **우선순위 구조(요구문에서 도출)**: 충돌 CONFIRMED 최우선 해제(SR-001) > 접근위험 잠금·억제(SR-002, 단 10초 내 재입력 시 override는 FR-003) > 입력 유효성/freshness 검사로 인한 degraded(SR-003) > sensor_fault 시 직전 출력 유지(SR-004) > 화재/과온/성인탑승 강제해제(FR-005) > ISOFIX 강제잠금(FR-006) > ignition-off 초기화(FR-007) > 차속 기반 자동잠금(FR-002) > 일반 운전자 명령(FR-001). 이 우선순위 체인이 전체 요구를 관통하므로 아키텍처는 처음부터 "규칙을 추가해도 기존 규칙을 건드리지 않는" 확장 가능한 구조(OCP)로 설계해야 한다.
- **공급자 게이트(§8)**: G1 요구사항(BL-SWR-1.0) → G2 설계(BL-SWA-1.0/BL-SWD-1.0) → G3 구현·통합(BL-CODE-UT-1.0/BL-INT-1.0) → G4 SW 검증(BL-VER-1.0).
- **추적성(§9)**: OEM 요구 13건이 이미 하위 요구 ID **SWR-001~021**에 사전 매핑되어 있다. 요구사항 분석 시 이 ID를 새로 부여하지 않고 **그대로 재사용**한다. (단, SWR-019는 매핑표에 나타나지 않음 — §5 질문 참조)

## 2. 게이트 ↔ 서브에이전트/스킬 매핑

| OEM Gate | 산출물 | 서브에이전트 | 스킬 |
|---|---|---|---|
| G1 요구사항 | SWR 요구사항 명세, Use Case, 추적매트릭스 | `requirements-analyst` | `requirements-engineering` |
| G2 설계 | 아키텍처 + 상세설계, UML | `architecture-designer` → `detailed-designer` | `architecture-design`, `detailed-design` |
| G3 구현·통합 | 소스, SBOM, 단위/통합시험 | `Coding` → `integration-tester` | `implementation`, `tdd`, `integration-test` |
| G4 SW 검증 | 요구 기반 PC/SIL·Web 시스템시험 결과 | `sw-system-tester`(케이스 설계) → `Coding`(실행·판정) | `sw-system-test` |

## 3. Phase 분할 및 근거

우선순위 체인이 요구 전체에 걸쳐 있어, 안전 핵심 로직을 먼저 세우고 그 위에 기능을 "규칙 추가" 방식으로 쌓는 순서로 나눈다. 각 Phase의 **분석** 단계는 그 Phase가 다루는 OEM 요구만 정제하지만, Phase 1의 분석 단계에서는 전체 SWR-001~021 추적성 골격(ID·상위 매핑·한 줄 요약)을 함께 초안해 이후 Phase의 분석이 그 골격을 채워나가는 방식으로 진행한다(재작업 방지).

### Phase 1 — 안전 핵심(ASIL B) 및 아키텍처 기반
- **범위**: OEM-SR-001~004, 관련 인터페이스 IF-001·IF-002·IF-003·IF-009(sensor_fault)
- 분석: 위 4건에 대응하는 SWR 요구사항 정제 + 전체 SWR-001~021 추적성 골격 초안
- 설계: 아키텍처 골격 확정(입력 검증기, 확장 가능한 우선순위 규칙체인 결정엔진, 상태 저장소, 출력 인터페이스 경계) + Phase 1 대상 상세설계
- 구현: TDD로 입력 유효성/freshness/degraded, sensor_fault 유지, 충돌 우선 해제, 접근위험 잠금·억제 구현
- 테스트: 단위(TDD, branch coverage 100%) + 통합(입력검증기↔결정엔진↔출력) + 이 범위의 시스템시험

### Phase 2 — QM 기능 계층
- **범위**: OEM-FR-001, FR-002, FR-003, FR-005, FR-006, FR-007, 인터페이스 IF-004·IF-005·IF-007·IF-008·IF-009(ignition_on)
- 분석: 대응 SWR 요구사항 정제(우선순위 규칙 추가분 정의)
- 설계: Phase 1 결정엔진 규칙체인에 QM 규칙 추가(아키텍처 재설계가 아닌 확장) + 상세설계 갱신
- 구현: TDD, Phase 1 회귀 포함
- 테스트: 단위 + 통합(회귀 포함) + 시스템시험(이 범위)

### Phase 3 — 표시·관측·Web 계층
- **범위**: OEM-FR-004, NFR-001, NFR-002, 인터페이스 IF-006, Web 시뮬레이터
- 분석: 대응 SWR 요구사항 정제
- 설계: 상태조회 API, 이벤트 로그(휘발성 100건 링버퍼), Web 시뮬레이터 화면/입력 설계
- 구현: TDD, 결정론적 재생(동일 입력·초기상태 → 동일 해시) 보장 로직
- 테스트: 단위 + 통합 + 시스템시험(이 범위) + Web 시뮬레이터 시나리오

## 4. Git 워크플로우

- Phase당 4개 브랜치: `phase-N-analysis`, `phase-N-design`, `phase-N-implementation`, `phase-N-test` — 이전 단계 PR이 `main`에 병합된 뒤 다음 단계 브랜치를 그 위에서 분기한다.
- 각 브랜치 산출물이 완성되면 PR을 열고, PR 본문에 해당 서브에이전트·산출물 경로·OEM 게이트 매핑을 기록한다.
- 사용자가 PR을 리뷰하고 승인하면 Squash merge로 `main`에 병합(`CI / quality-gates` 통과 필수)하고, 다음 단계로 진행한다.

## 5. 확인된 사항 (2026-09-18 결정)

- [x] Python 실행 버전: **3.12**(OEM 사양서 §3 고객 요구를 따름, 2026-09-18 정정). `CLAUDE.md`·CI 워크플로우·`implementation`/`tdd` 스킬·`Coding` 에이전트를 모두 3.12로 갱신했다.
- [x] SWR-019 결번: **의도된 결번으로 보고 SWR-001~021 중 019를 제외한 20개**로 요구사항 분석을 진행한다. 근거 없는 요구를 임의로 만들지 않는다.
- [x] Phase당 Git 단위: **단계별(분석/설계/구현/테스트) 브랜치·PR 4개**. §4 워크플로우 그대로 진행한다.
- [x] G4 시스템 테스트 실행·판정: **`sw-system-tester`는 케이스 설계까지**, **`Coding` 서브에이전트가 그 케이스의 실행과 Pass/Fail 판정**을 겸한다. `CLAUDE.md`와 `Coding` 에이전트 정의에 반영.

## 6. 진행 로그

| Phase | 단계 | 상태 | PR | 병합일 |
|---|---|---|---|---|
| - | - | 계획 수립 | - | - |
